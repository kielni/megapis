var log4js = require("log4js"),
    log = log4js.getLogger("megapis-worker"),
    workerUtil = require("megapis-worker-util");

module.exports.requiredConfigKeys = [""];

module.exports.run = function(config) {
    // go get stuff
};


